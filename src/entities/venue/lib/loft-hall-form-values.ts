/** Общие поля формы выбора площадок: источник правды — halls, lofts производное. */
export type LoftHallFormValues = {
  /** id лофтов (строками) — производное от выбранных залов. */
  lofts: string[]
  /** id залов (строками) — источник правды. */
  halls: string[]
}
