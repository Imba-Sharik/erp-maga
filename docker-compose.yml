services:
  app:
    build:
      context: .
      args:
        VITE_API_URL: ${VITE_API_URL:?Set VITE_API_URL in .env on the server}
        VITE_USE_MOCKS: ${VITE_USE_MOCKS:-false}
    ports:
      - "127.0.0.1:5173:80"
