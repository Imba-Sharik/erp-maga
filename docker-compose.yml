services:
  app:
    build: .
    command: sh -c "pnpm exec kubb generate && pnpm exec vite --host 0.0.0.0 --port 5173"
    ports:
      - "127.0.0.1:5173:5173"
    volumes:
      - .:/app
      - node_modules:/app/node_modules

volumes:
  node_modules:
